# 待确认项

以下事项不会阻塞当前前端、API 骨架和规则分析继续使用，但在进入更完整的生产级实现前最好确认。

## 1. 模型接入方案

当前状态：

- `/api/analyze` 已经接入真实抓取
- 当前分析层已支持规则分析和平台内置 LLM 深度分析
- 当前模型通过服务端环境变量注入
- LLM 不可用时会自动回退到规则分析

待确认：

- 后续是否继续固定当前平台内置模型
- 是否需要支持多模型切换
- 正式环境的 Key 管理方式

默认建议：

- 先保持平台内置模型统一输出
- 正式环境继续通过后端环境变量注入

## 2. 数据库存储方案

当前状态：

- API 当前使用项目内 JSON 持久化存储
- 已可完成最近分析、收藏和详情查询的基础闭环

待确认：

- 生产环境是否确定使用 Postgres
- 本地开发是否接受先用 SQLite / Docker Postgres 过渡
- 数据库连接字符串的提供方式

默认建议：

- 生产使用 Postgres
- 本地先接一套最小 Postgres 或 SQLite 兼容层

## 3. 认证与用户体系

当前状态：

- 当前收藏和灵感库是单用户原型态

待确认：

- 是否需要首版登录系统
- 是否要区分游客与登录用户
- 是否要在 MVP 就做用户隔离

默认建议：

- 先不做复杂登录
- 先保证单用户闭环可用

## 4. 真实抓取覆盖边界

当前状态：

- 已支持首页抓取
- 已支持自动发现部分关键页面
- 已支持基础文案、CTA、定价信息抽取

待确认：

- 首版是否需要支持受限站点 / 强前端渲染站点
- 是否要尽快接入 Playwright 兜底

默认建议：

- 先覆盖公开营销站
- 后续再接 Playwright 兜底
