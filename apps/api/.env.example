PORT=3001
# Platform built-in LLM configuration (server side only)
LLM_API_KEY=
LLM_MODEL=
LLM_BASE_URL=https://api.openai.com/v1
LLM_TIMEOUT_MS=45000
# Optional: override full completion endpoint if using an OpenAI-compatible proxy
# LLM_ENDPOINT=https://your-endpoint.example.com/v1/chat/completions
