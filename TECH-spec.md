# AI Website Analyzer 技术实现文档

## 1. 文档目标

本文档用于说明 AI Website Analyzer 的技术实现思路，重点回答以下问题：

- 网站数据如何抓取
- 可以获取到哪些数据
- 哪些数据无法稳定获取
- 分析结果如何生成
- 首版推荐使用什么技术栈
- 系统整体链路应该如何设计

本文档以“先做可落地版本，再逐步增强”为原则，优先服务原型落地和 MVP 开发。

## 2. 技术目标

本项目的技术目标不是构建一个通用型全网爬虫，而是实现一个“面向网站商业分析”的抓取与理解系统。

核心目标：

- 输入一个网站 URL
- 自动抓取该网站公开页面中的关键信息
- 提取对商业判断有价值的结构化内容
- 结合 LLM 输出大白话分析结果
- 支持结果存储、收藏和回看

## 3. 系统分层

整体上建议分成四层：

### 3.1 输入层

- 用户输入网站 URL
- 系统校验 URL 合法性
- 系统创建一次分析任务

### 3.2 抓取层

- 获取目标网站首页 HTML
- 自动发现关键信息页面
- 进一步抓取 Pricing、About、FAQ 等页面
- 获取页面文本、标题、链接、按钮和价格信息

### 3.3 分析层

- 对抓取内容做清洗和结构化提取
- 将结构化内容传给 LLM
- 生成产品、用户、痛点、收费和市场判断

### 3.4 存储与展示层

- 保存原始抓取结果
- 保存结构化分析结果
- 保存收藏和最近分析记录
- 在前端结果页和灵感库中展示

## 4. 网站抓取技术方案

网站抓取建议采用“轻量抓取优先，浏览器抓取兜底”的策略。

### 4.1 方案一：HTTP 抓取 + HTML 解析

#### 推荐技术

- `fetch`
- `axios`
- `cheerio`

#### 适用场景

- 静态网站
- 普通营销官网
- 大部分内容直接写在 HTML 里的站点

#### 能做的事情

- 获取完整 HTML
- 解析标题、描述、H1、段落文本
- 提取页面链接
- 提取按钮和 CTA 文案
- 自动发现定价页、关于页、FAQ 页

#### 优点

- 快
- 成本低
- 易于部署
- 适合首版大多数网站分析

#### 缺点

- 遇到前端渲染页面时，内容可能不完整
- 有些动态内容拿不到

### 4.2 方案二：无头浏览器抓取

#### 推荐技术

- `Playwright`

#### 适用场景

- React / Next.js / Vue 等前端渲染网站
- 需要等待 JavaScript 执行后才能显示完整内容的页面
- 需要截图或模拟真实浏览器访问的站点

#### 能做的事情

- 渲染页面后再提取文本
- 获取真实可见内容
- 获取页面截图
- 采集按钮、弹窗、表单、定价表格内容
- 进入子页面继续抓取

#### 优点

- 对现代网站兼容性更强
- 获取内容更完整
- 可以同时拿到视觉截图和 DOM 结构

#### 缺点

- 更慢
- 更耗 CPU 和内存
- 并发成本更高

### 4.3 推荐抓取策略

首版推荐策略：

1. 先用 `fetch + cheerio` 抓首页
2. 如果文本内容过少、结构异常或明显是前端渲染站点，再切换到 `Playwright`
3. 继续自动发现并抓取关键信息页面
4. 对页面内容做清洗和结构化提取

这种方式的优点是：

- 速度和成本可控
- 抓取成功率较高
- 适合 MVP 阶段快速验证

## 5. 建议抓取的页面范围

首版不建议全站递归抓取，建议聚焦和商业判断最相关的页面。

### 5.1 必抓页面

- `/`
- `/pricing`
- `/about`
- `/features`
- `/faq`

### 5.2 建议尝试抓取页面

- `/use-cases`
- `/solutions`
- `/enterprise`
- `/contact`
- `/blog`
- `/docs`

### 5.3 页面发现方式

- 解析首页导航栏链接
- 解析 footer 链接
- 识别常见关键词链接

建议识别关键词：

- pricing
- plans
- about
- features
- faq
- use-cases
- solutions
- enterprise
- contact

## 6. 可获取的数据范围

### 6.1 可稳定获取的公开页面数据

这部分最适合首版实现，也是支撑分析结果的主要数据源。

#### 页面基础信息

- 页面标题 `title`
- 页面描述 `meta description`
- canonical URL
- Open Graph 标题和描述

#### 页面内容信息

- H1 / H2 / H3
- Hero 区文案
- 核心功能介绍
- 功能模块标题
- FAQ 内容
- CTA 文案
- 导航文案
- Footer 文案

#### 商业信息

- 套餐名称
- 套餐价格
- 月付 / 年付文案
- 免费试用信息
- 免费版信息
- 企业版入口
- Demo 预约入口
- 联系销售入口

#### 交互与结构信息

- 主按钮文字
- 链接结构
- 注册入口
- 登录入口
- 联系方式
- 社媒链接

#### 媒体信息

- Logo URL
- 页面截图
- 关键视觉素材 URL

### 6.2 可通过规则提取或推断得到的信息

这类数据不是网站直接声明的，但可以通过页面结构和文案判断出来。

- 网站是否偏个人用户
- 网站是否偏企业用户
- 是否是自助注册型 SaaS
- 是否是销售驱动型产品
- 是否有模板市场倾向
- 是否有 SEO 获客迹象
- 是否强调效率提升
- 是否强调降本增效
- 是否强调行业垂直场景

### 6.3 可通过 LLM 语义分析得到的信息

这类数据是系统的核心价值所在，属于“基于页面内容的理解结果”。

- 网站到底是做什么的
- 它主要服务谁
- 它解决了什么痛点
- 它的核心卖点是什么
- 它适合什么使用场景
- 它为什么能收费
- 用户为什么愿意付费
- 它更像工具型、平台型还是服务型产品
- 这个方向是否还有市场机会
- 如果用户要做类似产品，应该如何切入

## 7. 不容易获取或无法稳定获取的数据

以下数据通常不能仅靠抓取公开页面稳定获取。

- 真实流量
- 真实 DAU / MAU
- 真实收入
- 真实付费人数
- 真实转化率
- 广告投放预算
- 用户留存数据
- 企业内部经营指标

这类数据如果后续有需要，可以通过以下方式补强：

- 接第三方数据服务
- 结合公开榜单和评论数据做估算
- 通过社媒、评论和案例页做侧面判断

因此，产品输出中建议明确区分三类信息：

- 页面明确可见
- 基于页面的高概率判断
- 基于商业逻辑的策略建议

## 8. 数据清洗与结构化提取

抓到网页后不能直接全部丢给模型，建议先做一层预处理。

### 8.1 清洗目标

- 去掉无关脚本和样式
- 去掉重复导航和噪声信息
- 保留核心正文、标题、按钮、价格和 FAQ
- 控制文本长度，避免传给模型的内容过大

### 8.2 建议提取字段

#### 网站级字段

- `site_url`
- `site_name`
- `site_domain`
- `site_title`
- `site_description`
- `language`

#### 页面级字段

- `page_url`
- `page_type`
- `page_title`
- `headings`
- `main_text`
- `cta_texts`
- `links`
- `pricing_blocks`
- `faq_items`

#### 媒体字段

- `logo_url`
- `screenshot_url`

#### 抓取元信息

- `fetch_method`
- `http_status`
- `fetched_at`
- `rendered`

## 9. LLM 分析层设计

### 9.1 分析目标

模型接收清洗后的结构化内容，输出一份偏创业判断的分析结果。

### 9.2 建议输出字段

- `summary_one_liner`
- `what_it_does`
- `target_users`
- `core_pain_points`
- `value_props`
- `pricing_model`
- `pricing_notes`
- `why_people_pay`
- `business_model_guess`
- `market_opportunity`
- `competition_level`
- `build_recommendation`
- `go_to_market_suggestions`
- `evidence`

### 9.3 输出风格要求

- 用大白话
- 少术语
- 先结论，后解释
- 强调创业者可执行判断

### 9.4 输出可信度分层

建议模型输出时带上信息层级：

- `explicit`：页面明确写了
- `inferred`：根据页面内容推断
- `strategy`：给用户的建议

## 10. 首版技术架构建议

### 10.1 前端

推荐方案：

- `Next.js`
- `React`
- `Tailwind CSS`

前端主要负责：

- 首页输入 URL
- 展示分析进度
- 展示结果页
- 收藏与灵感库页面

### 10.2 后端

推荐方案：

- `Next.js API Routes` 或独立 `Node.js` 服务

后端主要负责：

- 接收分析请求
- 触发抓取任务
- 提取和清洗页面数据
- 调用 LLM
- 保存分析结果
- 提供收藏和查询接口

### 10.3 抓取层

推荐方案：

- `axios` / `fetch`
- `cheerio`
- `Playwright`

### 10.4 数据库

推荐方案：

- `Postgres`

也可以首版用：

- `Supabase`

数据库主要存：

- 分析任务
- 原始抓取页面
- 结构化结果
- 收藏记录
- 最近分析记录

### 10.5 模型层

推荐方案：

- 支持结构化输出的 LLM

模型主要负责：

- 商业语义理解
- 大白话解释
- 输出结构化分析结果

## 11. 推荐处理链路

一次分析任务建议按照以下流程执行：

1. 用户输入 URL
2. 服务端校验 URL
3. 创建分析任务
4. 先抓取首页 HTML
5. 判断是否需要切换到 Playwright
6. 发现并抓取关键页面
7. 清洗页面内容
8. 提取标题、价格、CTA、FAQ 等字段
9. 组织成结构化输入
10. 交给 LLM 分析
11. 保存结果
12. 返回前端展示

## 12. 首版数据库对象建议

### 12.1 analysis_jobs

- `id`
- `url`
- `domain`
- `status`
- `created_at`
- `finished_at`

### 12.2 crawled_pages

- `id`
- `analysis_job_id`
- `page_url`
- `page_type`
- `title`
- `main_text`
- `raw_html`
- `http_status`
- `fetch_method`
- `created_at`

### 12.3 analysis_results

- `id`
- `analysis_job_id`
- `site_name`
- `summary_one_liner`
- `what_it_does`
- `target_users`
- `core_pain_points`
- `pricing_model`
- `pricing_notes`
- `why_people_pay`
- `market_opportunity`
- `build_recommendation`
- `evidence`
- `created_at`

### 12.4 bookmarks

- `id`
- `user_id`
- `analysis_result_id`
- `site_url`
- `site_name`
- `note`
- `created_at`

### 12.5 recent_views

- `id`
- `user_id`
- `analysis_result_id`
- `viewed_at`

## 13. 首版实现边界建议

首版建议聚焦以下技术目标：

- 稳定抓到首页和部分关键页面
- 稳定产出核心商业分析字段
- 稳定保存结果和收藏记录

首版不建议过早做：

- 全站递归爬取
- 高并发分布式抓取
- 复杂反爬策略
- SEO 和流量类外部数据接入
- 多网站自动对比
- 太复杂的评分系统

## 14. 风险与注意事项

### 14.1 网站反爬

- 部分网站会限制自动访问
- 首版应优先覆盖公开营销站，不追求全覆盖

### 14.2 页面结构不统一

- 定价页结构差异很大
- 需要规则提取和模型理解结合

### 14.3 模型幻觉

- 模型可能会对收费或市场做过度推断
- 需要证据字段和可信度分层

### 14.4 成本控制

- Playwright 成本较高
- 建议轻量抓取优先，浏览器抓取兜底

## 15. 一句话结论

这个产品的技术核心不是“把整个网站都爬下来”，而是“抓到对商业判断真正有价值的信息，再通过结构化提取和 LLM 分析，把网页内容转成创业者能直接使用的结论”。
